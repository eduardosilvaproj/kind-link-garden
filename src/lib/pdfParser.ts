import { Transacao, Config, Cidade, TipoDestino } from "../types";

// Note: This is a placeholder since the request specifies that 100% of the data 
// should be pre-loaded in the demo. However, we keep the structure for compatibility.
export const parseC6PDF = async (file: File, config: Config): Promise<Transacao[]> => {
  return [];
};
